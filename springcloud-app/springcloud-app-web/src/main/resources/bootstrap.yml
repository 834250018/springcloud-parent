spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: springcloud-config
      label: master
      name: springcloud-app-web
      profile: dev
  application:
    name: springcloud-app-web
  profiles:
    active: dev
eureka:
  client:
    service-url:
      # 注册中心地址
      defaultZone: http://127.0.0.1:8080/eureka/
